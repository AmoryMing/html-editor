---
name: html-edit
description: 可视化编辑本地 HTML 设计稿。Use when 用户要手改 HTML 设计稿、跨版本/跨文件复制粘贴卡片或区块、删除/重排 UI 元素、原位插入文字或截图、把 Claude 动态 artifact（bundler/React/babel）冻结成静态 HTML、保存手工校准的基准版。Triggers：改HTML、编辑设计稿、换卡片、拼版本、删卡片、插截图、冻结、静态化、固化、手动校准、html编辑器、/html-edit。
---

# html-edit · 本地 HTML 可视化编辑器

零依赖 Node 工具，解决三类问题：
1. **跨版本拼装** — 从 A 版复制一个卡片/区块，粘贴替换 B 版里的对应块（同源 localStorage 剪贴板，多标签页互拷）。
2. **手工校准** — 双击直接改文字、删除元素、上下移动排序、原位插入文字段落或剪贴板截图，保存自动备份。
3. **动态稿静态化** — Claude artifact 下载的 HTML 是 bundler 包（React 运行时渲染，源码里没有卡片标记），先「冻结另存」成纯静态 HTML，再做上述编辑。冻结时剥离全部 script，CSS 原样保留（设计稿 CSS 都是内联 style 块，冻结后视觉不变）。

## 启动

```bash
node ~/.claude/skills/html-edit/server.mjs "<HTML所在目录>" --port 8787
# 然后浏览器打开 http://localhost:8787/ （端口被占用会自动 +1，以终端输出为准）
open http://localhost:8787/
```

索引页列出全部 HTML 并标注：**动态 React**（先冻结）/ **静态**（直接编辑）。点文件名进入编辑模式（URL 即 `文件路径?hx=1`）。

## 工具条操作

- 模式徽章自动判定：`动态页·取材/冻结`（保存=另存 `<原名>·静态.html`，不动源文件）/ `静态页·可编辑`（保存=覆盖，旧版自动备份到 `<根目录>/.bak/<名>.<时间戳>.html`）。
- 动态页默认「交互中」（可先点页面 tab 摆好要冻结的状态），点按钮或 **Alt+点击** 切到选择。
- 单击选中（紫框）→ `↑父级` 逐级扩大选区；**选中后按住拖拽**＝插入线落位移动；`点位粘贴`＝复制后指哪插哪；`复制` / `替换`·`前插`·`后插`（粘贴）/ `删除` / `▲▼` 排序 / `+文字` / `+截图`（读剪贴板图片，失败回退文件选择，data-URL 内嵌）。
- **标注层（HTML PRD 场景）**：`✏标注` 把选中元素标为标注（橙虚框，工作稿保留）；`👁` 隐藏/显示预览；`交付另存` 导出剥离全部标注的 `·交付.html`。API：`__hx.note()` / `__hx.deliver()`。
- **评论**：选中 → `💬评论` 右下面板发表，角标钉定位，存服务端 `.comments/` 边车，12s 轮询同步，不进文档。
- **双击**任意文字 → 原位 contenteditable 编辑，点外部或 Esc 结束。
- `撤销`（Cmd/Ctrl+Z，30 步）；Esc 取消选中；Delete 删除选中。

## 跨版本换卡 SOP（例：把 v5 的照面卡片换进规格文档）

1. 索引页打开 **来源版本**（动态则先冻结另存，打开静态版）。
2. 选中要搬的卡片（点卡片内部 → `↑父级` 到整卡边界）→ `复制`。
3. 新标签页打开 **目标文档**（静态）→ 选中要被换掉的旧卡 → `替换` → `保存 ✓`。
4. 文字微调：双击改字 → 保存。改坏了去 `.bak/` 找回。

## Claude 驱动（程序化 API）

页面暴露 `window.__hx`，可用 preview_eval / Chrome MCP 代用户操作：
`__hx.select('.cpc')`（CSS 选择器）→ `__hx.copy()` → 导航到目标页 → `__hx.select(目标)` → `__hx.paste('replace'|'before'|'after')` → `await __hx.save()`（返回 `{ok, savedTo, backup}`）。`__hx.serialize()` 取当前清理后的完整 HTML。

注意：冻结会丢失 JS 交互（tab 切换等），定稿基准/规格文档本就应是纯静态；要保留交互的演示稿保留原动态文件即可（冻结另存不覆盖源文件）。
